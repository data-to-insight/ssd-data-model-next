# # uncomment if re-applying CI workflow
# # last updated 080825 RH

# name: Build & Deploy MkDocs 

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: pages
#   cancel-in-progress: true

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"

#       # System deps for graphviz and pygraphviz
#       - name: Install OS packages (Graphviz)
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y graphviz libgraphviz-dev pkg-config

#       - name: Install Py deps
#         run: |
#           python -m pip install --upgrade pip
#           # Your project deps
#           pip install -r requirements.txt
#           # Ensure pygraphviz can build against system graphviz
#           pip install pygraphviz

#       # OPTIONAL: if Poetry managing project, uncomment:
#       # - name: Install Poetry
#       #   run: pip install poetry && poetry --version

#       # Generate artifacts BEFORE mkdocs build
#       # (adjust these to your actual script paths/names)
#       - name: Generate ERDs (DOT + images)
#         run: |
#           PYTHONPATH=. python scripts/docs.generate_domain_erds.py
#           PYTHONPATH=. python scripts/docs.generate_schema_dot.py

#       - name: Generate ERD Markdown pages
#         run: |
#           PYTHONPATH=. python scripts/docs.generate_erd_md_pages.py

#       - name: Build MkDocs
#         run: mkdocs build --strict

#       - name: Upload Pages artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./site

#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - id: deployment
#         uses: actions/deploy-pages@v4
